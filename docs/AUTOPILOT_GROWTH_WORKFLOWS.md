# Autopilot growth workflows

The production executor in CrowdRelay remains the decision authority. n8n is an adapter layer.

The bundle contains real Gmail/form outreach, inbound reply correlation, Spotify growth execution, Bandsintown growth execution, verified external free-route execution, and a daily growth KPI digest.

All exported workflows are inactive and disable execution-data persistence. Production activation must use the existing n8n manifest/attestation/smoke path.
